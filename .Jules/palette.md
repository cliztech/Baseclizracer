## 2024-05-23 - Interactive Element Semantics
**Learning:** Replaced non-semantic `<span>` with `<button>` for mute control. This improved accessibility (keyboard, screen reader) but required careful CSS resets to maintain visual fidelity.
**Action:** When converting non-semantic elements to semantic ones, always verify default browser styles are reset and add focus indicators.
